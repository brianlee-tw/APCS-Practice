{
  "name": "apcs-diagnostic-site",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate:content": "node scripts/validate-content.mjs",
    "validate:ui": "node scripts/validate-ui.mjs",
    "validate:free7": "node scripts/validate-free-7day.mjs",
    "check": "npm run validate:content && npm run validate:ui && npm run validate:free7 && npm test"
  }
}
